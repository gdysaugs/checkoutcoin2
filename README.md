# checkoutcoin2

Cloudflare Pages 用の `checkoutcoins2.win` 初期リポジトリです。

## Local preview (optional)

```bash
python3 -m http.server 8787
```

## Manual deploy

```bash
npx wrangler pages deploy . --project-name checkoutcoin2 --branch main
```